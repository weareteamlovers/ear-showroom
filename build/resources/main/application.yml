server:
  port: 8080

spring:
  thymeleaf:
    cache: false
  datasource:
    url: jdbc:h2:mem:showroom
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

github:
  username: weareteamlovers
  token: ${GITHUB_TOKEN:}